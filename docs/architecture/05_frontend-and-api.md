# Frontend and API Architecture

This document explains the Streamlit UI layout, interaction model, and the
FastAPI endpoints used by the frontend and external clients.

TODO: Populate this file fully from the `Test` specification.
